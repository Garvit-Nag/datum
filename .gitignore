# Environment variables — never commit secrets
.env
.env.local
.env.*.local
backend/.env

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/
.coverage
htmlcov/

# Poetry — lock file IS committed for reproducibility
# pyproject.toml and poetry.lock are tracked

# Node / Next.js
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
next-env.d.ts

# OS
.DS_Store
Thumbs.db

# IDEs
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker
*.pid

# Uploaded files (uploads happen to Supabase Storage, not local disk)
uploads/
tmp/

# Claude Code internals — not part of the submission
.claude/
CLAUDE.md

# Project docs and context files — not part of the submission
docs/
*.docx
*.pdf

# Root-level package-lock (only frontend/ has a valid one)
package-lock.json

# Old data directory (replaced by ground_truth.txt)
data/

# Tests — not part of submission
backend/tests/
